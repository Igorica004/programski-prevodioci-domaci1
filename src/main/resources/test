program personalni_program;

procedura podatak proizvod_podataka(podatak p; podatak l)
{
   posalji p puta l;
}
promenljive { postavi podatak p; }
pocni {
   p podesi proizvod_podataka(3;5);
   pisi(proizvod_podataka(p;p));
}
